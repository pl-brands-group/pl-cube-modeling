cubes:
  - name: dws_created_orders_aggby_country_sku
    sql_table: shopify_transform.dws_created_orders_aggby_country_sku
    data_source: default

    joins: []

    dimensions:
      - name: date
        sql: date
        type: time
        primary_key: true

      - name: is_ai_product
        sql: is_ai_product
        type: string
        primary_key: true

      - name: country
        sql: country
        type: string
        primary_key: true

      - name: spu_id
        sql: spu_id
        type: string
        primary_key: true

      - name: skc_id
        sql: skc_id
        type: string
        primary_key: true

      - name: sku_id
        sql: sku_id
        type: string
        primary_key: true

      - name: shop_url
        sql: shop_url
        type: string
        primary_key: true

      - name: brand
        sql: brand
        type: string
        primary_key: true

      - name: channel
        sql: channel
        type: string
        primary_key: true

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

